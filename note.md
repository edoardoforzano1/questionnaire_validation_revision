instead of having previous questionnaire against previoud template we can just find a way to either manually insert the specific round we want or to use template without having 2 entries.

path from temp where you put the questionnaire you want to analyse and the possible previous round, while template is automatically fetched. 

then you have just to update name of item in path